<form class="uploader-box" method="post" action="/api/attachments" enctype="multipart/form-data">
  <div class="uploader-box-input">
    <input class="uploader-box-file" type="file" name="attachments" id="uploader-file-input" multiple />
    <label for="uploader-file-input"><span class="glyphicon glyphicon-download-alt" aria-hidden="true"></span><br><span style="text-decoration: underline"><%= __('choose-file') %></span><span class="uploader-box-dragndrop"> <%= __('or-drag-here') %></span></label>
    <button class="uploader-box-button" type="submit"><%= __('upload') %></button>
  </div>
</form>
