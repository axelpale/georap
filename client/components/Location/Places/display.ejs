<% if (places.length > 0) { %>
  <% if (places[0] === 'Remote territory') { %>
    <%= __('remote-place') %>
  <% } else { %>
    <%= places.join(', ') %>
  <% } %>
<% } else { %>
  <%= __('yet-undefined-place') %>
<% } %>
