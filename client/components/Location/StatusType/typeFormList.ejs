<%
  locationTypes.forEach(function (t) {
    var isActive = (t === currentType);
%>
  <button data-type="<%= t %>" title="<%= t %>" class="georap-type-<%= t %> georap-tag <%= isActive ? 'georap-tag-active' : '' %>">
    <img src="<%= toSymbolUrl(t) %>" width="22" height="40">
  </button>
<%
  });
%>
