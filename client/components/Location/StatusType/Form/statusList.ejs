<%
  var activityClass = function (status) {
    if (status === currentStatus) {
      return 'georap-tag-active';
    }
    return '';
  };
%>

<% locationStatuses.forEach(function (status) { %>
  <button data-status="<%= status %>" class="location-status-<%= status %> georap-tag <%= activityClass(status) %>">
    <img src="<%= toTemplateUrl(status) %>" width="11" height="20">
    <%= __('location-status', status) %>
  </button>
<% }) %>
