<div class="entry-images-browser">
  <% attachments.forEach(function (attachment, i) { %>
  <img
    class="<%= i === 0 ? 'active' : '' %>"
    src="<%= attachment.thumburl %>"
    title="<%= attachment.filename %>"
    data-attachment-key="<%= attachment.key %>">
  <% }); %>
</div>
