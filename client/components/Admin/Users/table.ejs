<table class="table">
  <thead>
    <tr>
      <th><%= __('username') %></th>
      <th><%= __('role') %></th>
    </tr>
  </thead>
  <tbody>
  <% users.forEach(function (p) { %>
    <tr>
      <td><a href="/admin/users/<%= p.name %>"><%= p.name %></a></td>
      <td><%= __('user-role', p.role) %></td>
    </tr>
  <% }); %>
  </tbody>
</table>
