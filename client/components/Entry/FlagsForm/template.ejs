<div class="form-flags form-group">
  <label><%= __('flag-post-as') %>:</label>
  <div class="checkboxes">
  <% flagBoxes.forEach(function (flagBox) { %>
    <label class="flag-checkbox checkbox-inline">
      <input type="checkbox" value="<%= flagBox.name %>"<%= flagBox.checked ? ' checked="checked"' : '' %>>
      <span class="entry-flag-name"><%= __(flagBox.name) %></span>
      <%
        if (flagBox.description) {
      %><span class="text-muted">– <%= __(flagBox.description) %></span><%
        }
      %>
    </label>
  <% }) %>
  </div>
</div>
